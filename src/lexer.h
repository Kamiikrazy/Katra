#include <stdio.h>
#include <stdlib.h>
#include <memory.h>
#include <string.h>
#include "tokens.h"


std::vector<KToken> KLexer(std::string &source) {
    std::vector<KToken> tokens;
    std:vector<std::string> tokenList;

}
